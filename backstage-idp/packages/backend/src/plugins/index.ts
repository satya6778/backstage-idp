/*
 * plugins/index.ts
 * Central place to register and configure each Backstage plugin.
 * Import this from index.ts.
 */

// This file is intentionally minimal for the new backend system.
// Plugin registration now happens via backend.add() in index.ts.
// Add custom plugin configuration here as your platform grows.

export {};
